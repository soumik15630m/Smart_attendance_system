# Core
fastapi>=0.128.0
uvicorn[standard]
websockets>=14.0
python-dotenv

# DB + cache
sqlalchemy>=2.0.46
asyncpg
alembic>=1.13.0
redis[hiredis]>=5.0.0

# AI
insightface>=0.7.3
onnxruntime-gpu
opencv-python
numpy<2.0.0

# Utils
requests
httpx
pydantic-settings
pgvector
pydantic
tomli; python_version < "3.11"  # tomllib backport, stdlib only from 3.11+

# Security pins (transitive deps with known CVEs)
jaraco.context>=6.1.0   # CVE-2026-23949: path traversal via malicious tar archives
msgpack>=1.2.1          # GHSA-6v7p-g79w-8964: OOB read/crash on Unpacker reuse
setuptools>=78.1.1      # CVE-2025-47273: path traversal in PackageIndex
wheel>=0.46.2           # CVE-2026-24049: arbitrary code execution via malicious wheel
