# ==========================================
#  SERVER SIDE SETTINGS (API & Database)
# ==========================================

# 1. Database Connection (NeonDB / PostgreSQL)
# Format: postgresql+asyncpg://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=postgresql+asyncpg://neondb_owner:***********@ep-restless-***.aws.neon.tech/neondb

# 2. Redis Cache Settings
# If running locally (without Docker): use 'localhost'
# If running inside Docker: use 'redis' (the service name from docker-compose.yml)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# 3. Security (Critical Updates)
# Used for JWT/Session encryption
SECRET_KEY=change-this-to-a-secure-random-string
# API Key to prevent unauthorized access (The "v1" security fix)
API_KEY=my-secret-admin-key-2026

# 4. App Configuration
DEBUG=True
HOST=127.0.0.1
PORT=8000
LOCAL_ONLY=True
# 0.5 is standard for InsightFace. Lower = looser matching, Higher = stricter.
SIMILARITY_THRESHOLD=0.5
# Fixes the "wrong date" issue in logs (Use your local timezone)
TZ=Asia/Kolkata

# ==========================================
#  CLIENT SIDE SETTINGS (Camera Script)
# ==========================================
# These are used by camera_client.py to find the server

SERVER_IP=127.0.0.1
SERVER_PORT=8000
CAMERA_INDEX=0

# Optional: Override CUDA path if it's not in your system PATH
# CUDA_PATH_BIN=C:\Program Files\NVIDIA GPU Computing Toolkit\CUDA\v12.8\bin
